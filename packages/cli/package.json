{
  "name": "@cv-builder/cli",
  "version": "0.1.0",
  "description": "CV Builder CLI — evaluate a CV from the terminal and print a colour-coded score, dimensions, issues, and ATS verdict",
  "type": "module",
  "bin": {
    "cv-builder": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@cv-builder/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "keywords": [
    "cv",
    "resume",
    "cli",
    "job-search"
  ],
  "license": "MIT"
}
